---
phase: 3-multitenancy-auth-analysis
plan: "02"
type: execute
depends_on: ["03-01"]
files_modified:
  - .planning/phases/3-multitenancy-auth-analysis/AUTH.md
autonomous: true
---

<objective>
Propósito: Documentar el modelo de autenticación y autorización de cloud2 — cómo se autentican los usuarios, roles y estructura de permisos, evaluación de separabilidad del sistema auth, y reglas de autorización por módulo.

Salida: `.planning/phases/3-multitenancy-auth-analysis/AUTH.md`
</objective>

<context>
@.planning/phases/2-functional-module-identification/LIMITES-MODULOS.md
@.planning/phases/3-multitenancy-auth-analysis/MULTITENANCY.md
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Documentar el flujo de autenticación</name>
  <files>.planning/phases/3-multitenancy-auth-analysis/AUTH.md</files>
  <action>
    Usa `mcp__project2context__search_files` y `mcp__project2context__query_classes` para encontrar:
    - Guards de autenticación (config/auth.php)
    - Providers de usuario configurados
    - Controladores de login/logout/registro
    - Middlewares de autenticación
    - Paquetes de autenticación en composer.json (Laravel Sanctum, Passport, JWT, etc.)
    - Tabla de usuarios (modelo User)

    Crea AUTH.md con sección "## Flujo de Autenticación":
    - Método de autenticación: session-based / token-based (JWT) / OAuth2 / SSO / mixto
    - Providers configurados: qué tabla/modelo se usa para cada guard
    - Flujo de login: ruta → controlador → guard → modelo → respuesta
    - Persistencia: dónde se guarda la sesión (database, redis, cookies)
    - Tokens/sessions: tiempo de expiración si es detectable

    Cita rutas reales de archivos y valores reales de config/auth.php.
  </action>
  <verify>AUTH.md existe con sección "## Flujo de Autenticación" con datos reales de config/auth.php y controladores.</verify>
  <done>Flujo de autenticación documentado con datos reales del codebase.</done>
</task>

<task type="auto">
  <name>Tarea 2: Documentar roles y estructura de permisos</name>
  <files>.planning/phases/3-multitenancy-auth-analysis/AUTH.md</files>
  <action>
    Usa `mcp__project2context__search_files` y `mcp__project2context__query_classes` para encontrar:
    - Modelo de Roles (busca Role.php, tablas roles, role_user)
    - Modelo de Permisos (busca Permission.php, tablas permissions)
    - Paquetes de RBAC en composer.json (spatie/laravel-permission, etc.)
    - Policies en app/Policies/
    - Gates definidos en AuthServiceProvider

    Documenta en AUTH.md bajo "## Roles y Permisos":
    - Sistema de RBAC: paquete usado o implementación propia
    - Roles definidos: lista de roles conocidos (desde la BD/seeders o código)
    - Estructura de permisos: granularidad (por ruta, por modelo, por acción)
    - Cómo se asignan roles a usuarios: manual / automático / por tenant
    - Si los roles son globales o por tenant

    Si hay seeders de roles/permisos, extraer la lista desde database/seeders/ con P2C.
  </action>
  <verify>Sección "## Roles y Permisos" en AUTH.md con roles, permisos y mecanismo RBAC identificados.</verify>
  <done>Sistema de roles y permisos documentado con datos reales del codebase.</done>
</task>

<task type="auto">
  <name>Tarea 3: Evaluar separabilidad del sistema auth</name>
  <files>.planning/phases/3-multitenancy-auth-analysis/AUTH.md</files>
  <action>
    Basándote en las tareas 1-2 y en la lista de módulos (LIMITES-MODULOS.md):

    Para evaluar la separabilidad:
    Usa `mcp__project2context__find_referencing_symbols` en el modelo User y en las clases de Auth.
    Cuenta cuántos módulos referencian directamente al modelo User o a facades Auth/Gate.

    Documenta en AUTH.md bajo "## Evaluación de Separabilidad":
    - ¿Está el auth acoplado al monolito? (¿Cuántos controladores usan Auth::user() directamente?)
    - ¿Se puede extraer como servicio independiente? (Sí/Parcialmente/No — con justificación)
    - Dependencias que bloquean la extracción: lista de clases/tablas que harían difícil extraer auth
    - Ruta propuesta para la reescritura: "auth como servicio separado" / "auth embebido en cada módulo" / "auth compartido"
  </action>
  <verify>Sección "## Evaluación de Separabilidad" con evaluación basada en evidencia real del codebase.</verify>
  <done>Separabilidad del sistema auth evaluada con evidencia real.</done>
</task>

<task type="auto">
  <name>Tarea 4: Mapear reglas de autorización por módulo</name>
  <files>.planning/phases/3-multitenancy-auth-analysis/AUTH.md</files>
  <action>
    Usa `mcp__project2context__query_classes` para listar todas las Policies en app/Policies/.
    Usa `mcp__project2context__search_files` para encontrar uso de Gate::define(), @can, ->authorize() en controladores.

    Documenta en AUTH.md bajo "## Autorización por Módulo":
    Tabla: | Módulo | Policy/Gate | Permisos Específicos | Notas |

    Para módulos sin policies explícitas, indica si usan middleware de roles o verificación manual.
    Identifica módulos sin ninguna verificación de autorización — son riesgos de seguridad.

    Esta información informa la arquitectura de autorización en la reescritura modular.
  </action>
  <verify>Sección "## Autorización por Módulo" con policies y gates por módulo documentados.</verify>
  <done>Reglas de autorización mapeadas por módulo con evidencia real.</done>
</task>

</tasks>

<verification>
- [ ] AUTH.md con 4 secciones completas
- [ ] Flujo de autenticación documentado con datos reales
- [ ] Roles y permisos identificados con mecanismo real
- [ ] Separabilidad evaluada con evidencia
- [ ] Autorización mapeada por módulo
</verification>

<success_criteria>
- Modelo auth completamente documentado con datos reales del codebase
- Separabilidad del auth evaluada con justificación basada en código
- Autorización por módulo mapeada
</success_criteria>

<output>
Al completar, crear `.planning/phases/3-multitenancy-auth-analysis/SUMMARY-03-02.md`:

# Plan 03-02: Auth y Autorización — Resumen

## Logros
## Mecanismo Auth Detectado
## ¿Es Separable el Auth?
## Preparación para Fase 4
</output>
